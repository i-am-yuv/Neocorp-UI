stages:
  - build
  - deploy

build-frontend:
  stage: build
  image: node:18

  script:
    - npm install --legacy-peer-deps
    - npm ci --legacy-peer-deps
    - npm run build
  artifacts:
    name: "doneocards-jar-artifact"
    paths:
      - target/dist

deploy-frontend:
  stage: deploy
  image: alpine

  script:
    - ls
