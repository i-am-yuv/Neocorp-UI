// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  //apiurl: 'http://localhost:8222/api',
  apiurl: 'https://api.ifyn.com/neocorp/api', 
  apiurlNew: 'https://api.ifyn.com/neocorp/', 
  //apiurl:'http://localhost:8669/',
  docsurl: 'https://portal.neomaxer.com/docs/info',
  //apiurl: 'http:10.20.4.120:8222/api',
  //apiurl: 'http://10.0.0.112:8222/api'
  // apiurl: 'http://10.0.0.125:8222/api'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
